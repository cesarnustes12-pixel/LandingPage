# Vital Solution Landing Page

Welcome to the Vital Solution landing page repository. This project contains a simple and modern landing page designed to showcase the services and mission of Vital Solution.

## Project Structure

- `index.html`: The main HTML file for the landing page.
- `style.css`: Contains the styles for the landing page.
- `script.js`: Includes JavaScript functionality for the page.

## How to Use

1. Clone the repository:
   ```bash
   git clone https://github.com/cesarnustes12-pixel/Vital-solution.git
   # Fitovit — Landing Page

   Pequeña landing page para Fitovit (suplementos naturales). Esta carpeta contiene el HTML, CSS, JS y placeholders de imágenes usados por la maqueta.

   ## Estructura del proyecto

   - `index.html` — Página principal (maqueta en español).
   - `style.css` — Estilos (tema oscuro/claro con variables de color).
   - `script.js` — Comportamientos (toggle tema y enlace a WhatsApp).
   - `images/` — Placeholders y assets (hero, logo, productos, etc.).

   ## Placeholders incluidos

   - `images/hero-placeholder.svg` — imagen principal (hero) de ejemplo.
   - `images/logo-placeholder.svg` — logo de ejemplo.
   - `images/product-placeholder.svg` — placeholder para productos/servicios.
   - `images/probioticos.jpg`, `images/biotina.jpg`, `images/magnesio.jpg` — ejemplos añadidos automáticamente.
   - `images/README.md` — recomendaciones y convenciones para nombres de imágenes.

   ## Cómo probar localmente

   1. Abrir `index.html` en tu navegador (doble clic) para ver la maqueta rápidamente.

   O servirlo localmente (recomendado para evitar restricciones de CORS en algunos navegadores):

   ```powershell
   cd d:\LocalDevelopment\LandingPage
   python -m http.server 8000
   # Abrir http://localhost:8000 en tu navegador
   ```

   ## Qué he hecho hasta ahora

   - Añadí la estructura de la landing (hero, beneficios, productos, testimonios, FAQ, footer).
   - Implementé toggle de tema (claro/oscuro) y botón de WhatsApp funcional en `script.js`.
   - Añadí estilos base y placeholders dentro de `images/`.
   - Commit inicial registrado localmente.

   ## Siguientes pasos recomendados

   - Reemplazar los placeholders en `images/` por las imágenes finales (nombres sugeridos: `hero.jpg`, `logo.png`, `product-1.jpg`).
   - Afinar tipografías y microcopy (puedo aplicar la tipografía variable y ajustes visuales).
   - Preparar assets optimizados (webp/jpg) y añadir un pequeño pipeline de optimización si lo deseas.

   ## Contacto / notas

   Si quieres que suba las imágenes finales, dime los nombres y las subiré/reemplazaré en la maqueta. Para cualquier ajuste visual también puedo generar una rama y trabajar allí.

   ---

   _Archivo actualizado automáticamente por Copilot — commit seguirá a continuación._
